# Classical ML Concept Check

Answer without running code first.

1. Why can test accuracy be lower than training accuracy?
2. Why should the test set not guide hyperparameter tuning?
3. When can accuracy be misleading?
4. What changes when a logistic-regression classification threshold moves from 0.5 to 0.2?
5. Why does k-NN care about feature scaling?
6. Why can an unrestricted decision tree overfit?
7. What does a random forest gain by combining randomized trees?
8. How is boosting conceptually different from bagging?
9. What does an SVM margin mean?
10. What is the kernel trick trying to accomplish?
11. Contrast L1 and L2 regularization.
12. Explain bias and variance without using the words underfitting or overfitting.
13. Why should preprocessing be fitted separately inside each CV training fold?
14. When should you use grouped or time-aware splitting?
15. For a rare-event detector, what information does a confusion matrix reveal that accuracy hides?
